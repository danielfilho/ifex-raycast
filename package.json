{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "ifex-exif-viewer",
  "title": "Ifex Exif Viewer",
  "description": "View EXIF data from image files using the ifex CLI tool",
  "icon": "extension-icon.png",
  "author": "danielfilho",
  "categories": [
    "Media"
  ],
  "keywords": [
    "photography",
    "photo",
    "exif",
    "iptc",
    "metadata"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "ifex",
      "title": "View Exif Data",
      "description": "View EXIF data from files selected in Finder",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.83.1",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.2",
    "@types/node": "20.8.10",
    "@types/react": "18.3.3",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "lint:fix": "biome check --write ./src",
    "format": "biome format --write ./src",
    "check": "biome check ./src",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` or \\`ray publish\\`.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
